import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.indianstockwatchlist',
  appName: 'Mini AI stock advisor',
  webDir: 'dist/indian-stock-watchlist'
};

export default config;
